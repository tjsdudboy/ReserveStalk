spring:
  config:
    activate:
      on-profile: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ReserveStalk
    username: root
    password: admin123!
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  jwt:
    secret: qL9A/KhVgMZEtpjeikQJGHqhgm2/sLZu4HpAtjYQvxo=
server:
  servlet:
    context-path: /api
logging:
  level:
    org:
      springframework:
#        security: debug
        web: debug